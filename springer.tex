% Springer-Journal Latex Template Adopted for Use with Pandoc
% Jonas Schöley
% 2019-03-07

% !TeX program = pdfLaTeX
\documentclass[$classoption$]{svjour3} % smallcondensed, smallextended, twocolumn

\smartqed  % flush right qed marks, e.g. at end of proof

%%%% font/encoding %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage[utf8]{inputenc}
\usepackage{mathptmx}

%%%% maths %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{amsmath}

%%%% figures %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

$if(graphics)$
\usepackage{graphicx} % include external images
% -- We will generate all images so they have a width \maxwidth. This means
% -- that they will get their normal width if they fit onto the page, but
% -- are scaled down if they would overflow the margins.
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth
\else\Gin@nat@width\fi}
\makeatother
\let\Oldincludegraphics\includegraphics
\renewcommand{\includegraphics}[1]{\Oldincludegraphics[width=\maxwidth]{#1}}$endif$

%%%% localization %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% babel
\usepackage[english]{babel}         % document language/localization
\usepackage[htt]{hyphenat}          % hyphenation rules

%%%% links %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage[hyphens]{url}
\usepackage{hyperref}
\hypersetup{
  hidelinks,
  breaklinks=true,
  pdftitle={$title$}
}

%%%% bibliography %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{natbib}
\setcitestyle{aysep={}}
\bibliographystyle{spbasic}
%\newcommand{\doi}[1]{\href{http://www.dx.doi.org/#1}{\textcolor{blue}{doi:#1}}}

%%%% tables %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

% $if(tables)$
%   \usepackage{array,longtable,booktabs,multirow,lscape}
%
%   % -- This is needed because raggedright in table elements redefines \\:
%   \newcommand{\PreserveBackslash}[1]{\let\temp=\\#1\let\\=\temp}
%   \let\PBS=\PreserveBackslash
% $endif$

%%%% includes %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

$preamble$

$if(highlighting-macros)$
  $highlighting-macros$
$endif$

$for(header-includes)$
  $header-includes$
$endfor$

%%%% title, authors %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\title{$title$ $if(thanks)$\thanks{$thanks$} $endif$}
$if(subtitle)$ \subtitle{$subtitle$} $endif$

$if(titlerunning)$
    \titlerunning{$titlerunning$}
$endif$

%\author{ $for(authors)$ $authors.name$ \and $endfor$ }
\author{ $for(authors)$ $authors.name$ $sep$ $endfor$ }

$if(authorrunning)$
  \authorrunning{ $authorrunning$ }
$endif$

\institute{
    $for(authors)$
    $authors.name$ \at
    $if(authors.address)$ $authors.address$ $endif$\\
    $if(authors.email)$ \email{$authors.email$} $endif$
    $sep$
    $endfor$
}

\date{Received: date / Accepted: date}
% The correct dates will be entered by the editor

\journalname{$journalname$}

%%%% document %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}

\maketitle

\begin{abstract}
$abstract$
\\
\keywords{
    $for(keywords)$
    $keywords$ \and
    $endfor$
}
$if(PACS)$
    \PACS{
        $for(PACS)$
            $PACS$ \and
        $endfor$
    }
$endif$

$if(MSC)$
    \subclass{
        $for(MSC)$
            $MSC$ \and
        $endfor$
    }
$endif$

\end{abstract}

\def\spacingset#1{\renewcommand{\baselinestretch}%
{#1}\small\normalsize} \spacingset{1}

$body$

\bibliography{$for(bibliography)$$bibliography$$sep$,$endfor$}

\end{document}
